    <div class="class-body m-2">
        <% comments.each do |comment| %>
            <p>Comment#<%= comment.id %> by <%= comment.user.name %>: <%= comment.text %></p>
        <% end %>
    </div>    
