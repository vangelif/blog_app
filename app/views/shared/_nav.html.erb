<nav class="navbar navbar-expand-lg bg-dark border-bottom border-body" data-bs-theme="dark">
  <div class="container">

    <!-- Navbar brand -->
    <a class="navbar-brand" href="#">Your Blog App</a>

      <!-- Search form -->
      <form class="d-flex" role="search">
        <input class="form-control mx-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-warning" type="submit">Search</button>
      </form>
      
      <% if user_signed_in? %>
<span>Welcome back, <strong style="" class="logged-email"><%= current_user.email %></strong></span>
<a><%= link_to 'Sign Out', switch_user_path, class: 'btn-outline-warning no underline mx-2', style: 'text-decoration: none;' %></a>
<% end %>
    </div>
  </div>
</nav>
